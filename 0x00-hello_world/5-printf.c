#include <stdio.h>
	/**
	* main - Prints out a string to stdout.
	*main - entry point
	* Return: 0 if success.
	*/
	{
		printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
	}
